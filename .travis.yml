language: minimal

os: linux

services:
  - docker

# Build + test happen entirely inside the pinned Ubuntu 18.04 + LLVM 8.0.0
# container (see Dockerfile); Giri is never built on the host.
script:
  - docker build -t giri-llvm8 .
